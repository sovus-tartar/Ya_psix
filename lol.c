#include <stdio.h>
#include <stdlib.h>
struct recept
{
    unsigned long long timestamp;
    unsigned id;
    unsigned recept_id;
};

int main()
{
    int n, i, temp;
    struct recept *rec;
    temp = 0;
    scanf("%d", &n);
    calloc

    for (i = 0; i < n; i++)
    {
        scanf("%llu%u%u", rec.timestamp, rec.id, rec.recept_id);
        
    }

    return 0;
}