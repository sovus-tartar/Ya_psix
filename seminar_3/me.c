#include <stdio.h>
#include <stdlib.h>

int majority_element(int const * parr, unsigned len) {
    
    if (len == 1) 
        return parr[0];
    
    
    
    return -1;
}

int main() {
    int * arr;
    unsigned n;

    scanf("%u", &n);
}